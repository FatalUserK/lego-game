using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NEMS : MonoBehaviour
{
    // Start is called before the first frame update
    void OnServerConnect()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
